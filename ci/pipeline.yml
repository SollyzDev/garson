resources:
    - name: code
      type: git
      source: {uri: 'https://github.com/ishehata/garson'}

jobs:
    - name: test
      plan:
        - get: code
          trigger: true
        - task: test
          file: code/ci/tasks/test.yml
